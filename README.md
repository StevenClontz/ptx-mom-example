# MyOpenMath with PreTeXt-CLI

The result of

```
pretext devscript source/main.ptx -c mom -p publication/publication.ptx
pretext build
```

is deployed to <https://stevenclontz.github.io/ptx-mom-example/>
